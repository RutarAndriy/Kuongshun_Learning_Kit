// Метод ініціалізації програми
// Виконується один раз під час запуску програми

void setup() {

// Налаштовуємо пін, до якого підключений вбудований світлодіод, на вихід сигналу
pinMode(LED_BUILTIN, OUTPUT);

}

// Основний цикл програми
// Безкінечний повторювальний цикл

void loop() {

// Подаємо високий сигнал на пін
digitalWrite(LED_BUILTIN, HIGH);

// Затримка 5 сек.
delay(5000);

// Подаємо низький сигнал на пін
digitalWrite(LED_BUILTIN, LOW);

// Затримка 0.5 сек.
delay(500);

}
